import { LogLevel } from "../../../logger/LogLevel.js";
import { TaskEvent } from "../types.js";
/**
 * Internal class for CustomEvent.
 */
/**
 * @internal
 */
export declare class CustomEvent extends Event {
    /**
     * @internal
     */
    detail: any;
    constructor(message: TaskEvent, data: any);
}
/**
 * @internal
 */
export declare function getAccessToken(app: any): Promise<string | null>;
/**
 * @internal
 */
export declare function isValidMetadata(obj: any): boolean;
/**
 * @internal
 */
export declare function fetchWithRetry(url: string, options: any, maxRetryTime: number, app?: any): Promise<Response>;
/**
 * Internal class for providing utility functions to all the classes.
 */
/**
 * @internal
 */
export declare class Utils {
    private static readonly LOG_REDACTION;
    private static readonly MAX_LOG_DEPTH;
    private static isSensitiveLogKey;
    private static redactLogString;
    private static redactLogValue;
    /**
     * @static
     * @property {Function} baasTrace
     * Get the trace of the request and the function stack.
     * @param {Object} data
     */
    /**
     * @internal
     */
    static baasTrace(logLevel: LogLevel, ...data: any[]): void;
    /**
     * @static
     * @property {Function} checkResponse
     * Checks if the returned response has status code >= 200 & <= 299.
     * Throws error if the status code isn't in the specified range.
     * @param {Object} response
     */
    /**
     * @internal
     */
    static checkResponse(response: Response): void;
    /**
     * @static
     * @property {Function} baasLogger
     * Log redacted data to the console if ERROR.
     * @param {T} data
     */
    /**
   * @internal
   */
    static baasLogger(logLevel: LogLevel, ...data: any[]): void;
    /**
     * @static
     * @property {Function} parseJWT
     * Parses the content of the JWT token's body.
     * @returns {Object} JWT Token's content in json.
     */
    /**
   * @internal
   */
    static parseJWT(token: string): any;
}
//# sourceMappingURL=utils.d.ts.map