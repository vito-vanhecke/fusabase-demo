import { LogLevel } from "../../../logger/LogLevel.js";
import { UploadTaskSnapshot } from "../internal/uploadTask.js";
/**
 * Internal controller class for UploadTask.
 */
/**
 * @internal
 */
export declare class UploadTaskController {
    #private;
    private storageHelper;
    private abortController;
    private eventListener;
    private signal;
    private dataToSend;
    /**
     * @internal
     */
    uploadMetadata: any;
    /**
     * @internal
     */
    logLevel: LogLevel;
    /**
     * @internal
     */
    isMultipart: {
        totalSize: number;
        totalChunks: number;
        toSend: number;
        baseURL: string;
    } | null;
    constructor(metadata: any, storageHelper: any);
    private uploadSize;
    private maxUploadBytes;
    private assertUploadWithinLimit;
    private uploadBody;
    private uploadChunk;
    /**
     * @async
     * @property {Function} initUpload
     * Set necessary config for the upload process.
     * @param {UploadTaskSnapshot} snapshot
     * @param {Blob | any} data
     * @returns {Promise<void>}
     */
    /**
     * @internal
     */
    initUpload(snapshot: UploadTaskSnapshot, data: Blob | any): Promise<void>;
    /**
     * @async
     * @property {Function} abortUpload
     * Abort the upload process.
     * @returns {Promise<void>}
     */
    /**
     * @internal
     */
    abortUpload(): Promise<void>;
    /**
     * @internal
     */
    get eventTarget(): EventTarget;
    /**
     * @property {Function} setCallBacks
     * Set callbacks for the task.
     * @returns {void}
     */
    /**
     * @internal
     */
    setCallBacks(event: string, callbacks: any, abortController: AbortController): () => void;
    /**
     * @property {Function} fireEvent
     * Dispatches an event.
     * @returns {void}
     */
    /**
     * @internal
     */
    fireEvent(event: Event): void;
    /**
     * @async
     * @property {Function} continueUpload
     * Continue the upload process.
     * @param {UploadTaskSnapshot} snapshot
     * @returns {Promise<Object>}
     */
    /**
     * @internal
     */
    continueUpload(snapshot: UploadTaskSnapshot, access_token?: string | null): Promise<any>;
}
//# sourceMappingURL=uploadHelper.d.ts.map