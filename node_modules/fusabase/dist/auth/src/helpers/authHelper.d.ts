import { LogLevel } from "../../../logger/LogLevel.js";
import { IDCSConfig, ONPREMConfig } from "./config.js";
import { IdTokenResult } from "../types/idtoken.js";
/**
 * Internal Helper class for Auth for IDCS.
 * Mirrors the base/onprem helper; the IDCS config supplies the idcs URL segment.
 */
/**
 * @internal
 */
export declare class IDCSAuthHelper {
    private delegate;
    config: IDCSConfig;
    logLevel: LogLevel;
    ordsHostOrigin: string;
    /** @internal */
    private _app;
    constructor(config: IDCSConfig, logLevel: LogLevel, ordsHostOrigin?: string);
    /** @internal */
    _setApp(app: any): void;
    authenticateAndGetDetails(email: string, password: string): Promise<any>;
    authenticateUser(email: string, password: string): Promise<any>;
    reloadUser(user: any): Promise<any>;
    getUserDetails(access_token?: IdTokenResult): Promise<any>;
    registerUser(email: string, password: string, url: string): Promise<any>;
    base64UrlEncode(arrayBuffer: Uint8Array): string;
    generateCodeVerifier(length?: number): string;
    generateCodeChallenge(codeVerifier: string): Promise<string>;
    getRedirectCredentials(code: string, url: string): Promise<any>;
    socialLogin(url: string): Promise<any>;
    listenForAuthToken(popupWindow: any): Promise<any>;
    performSignOut(refresh_token: string): Promise<void>;
    signOutFromIDCS(): Promise<void>;
    revokeRefreshToken(refresh_token: string): Promise<void>;
    sendPasswordResetEmailHelper(email: string): Promise<void>;
    verifyPasswordResetCodeHelper(code: string): Promise<any>;
    confirmPasswordResetHelper(code: string, newPass: string, email: string): Promise<any>;
    signInWithCredentialHelper(credential: any): Promise<any>;
}
/**
 * Internal helper class for Auth for Onprem.
 */
/**
 * @internal
 */
export declare class ONPREMAuthHelper {
    config: ONPREMConfig;
    logLevel: LogLevel;
    ordsHostOrigin: string;
    /** @internal */
    private _app;
    constructor(config: ONPREMConfig, logLevel: LogLevel, ordsHostOrigin?: string);
    /** @internal */
    _setApp(app: any): void;
    authenticateAndGetDetails(email: string, password: string): Promise<any>;
    authenticateUser(email: string, password: string): Promise<any>;
    reloadUser(user: any): Promise<any>;
    getUserDetails(access_token?: IdTokenResult): Promise<any>;
    registerUser(email: string, password: string, url: string): Promise<any>;
    base64UrlEncode(arrayBuffer: Uint8Array): string;
    generateCodeVerifier(length?: number): string;
    generateCodeChallenge(codeVerifier: string): Promise<string>;
    getRedirectCredentials(code: string, url: string): Promise<any>;
    socialLogin(url: string): Promise<any>;
    listenForAuthToken(popupWindow: any): Promise<any>;
    performSignOut(refresh_token: string): Promise<void>;
    revokeRefreshToken(refresh_token: string): Promise<void>;
    sendPasswordResetEmailHelper(email: string): Promise<void>;
    verifyPasswordResetCodeHelper(code: string): Promise<any>;
    confirmPasswordResetHelper(code: string, newPass: string, email: string): Promise<any>;
    signInWithCredentialHelper(credential: any): Promise<any>;
}
//# sourceMappingURL=authHelper.d.ts.map